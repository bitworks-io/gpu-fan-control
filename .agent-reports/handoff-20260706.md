# Session Handoff — 2026-07-06

**Objective** — Ship v1.0 of the Lightweight AMD GPU Fan Control utility: automatic fan control targeting 65 °C core temp, multi-GPU support, and an installable Windows build.

**Current State** — Phases 1–4 complete and CI-green. Multi-GPU per-channel architecture, `GpuEnablement.Reconcile`, policy-driven control loop, restore-to-auto lifecycle, and the About/Preferences UI + live tray tooltip all compile on Windows CI. Latest feature commit `8247ca0` (safety fix) is green (CI run `28714260647`). 26/26 Core unit tests pass locally (net8.0). **Phase 5 (hardware verification on a real AMD PC) is the only remaining gate** — not started; needs physical hardware. The v1.0.0 release tag is intentionally **not cut** yet.

**Accepted Decisions**
- Target **core/edge** temp at 65 °C default (not junction/hotspot) — user directive.
- Fan **ceiling 85%** (hard cap, manufacturer guidance); **floor 20%** (no Zero-RPM).
- Multi-GPU: primary GPU on by default, additional GPUs opt-in. ADLX = full multi-GPU; legacy ADL = primary GPU only (avoids duplicate-display-adapter dedup).
- **Single-owner native session**: `FanControlChannelSet` owns one ADLX/ADL session; per-GPU channels borrow it; one control lock serializes all native access.
- Unsigned build now; signing hook gated for later.
- `gh` account must be `bitworks-io` (reverts to `tyria-tech`; re-switch on 404).
- SSH: remove any keychain directive (stock OpenSSH rejects both `UseKeychain`/`AddKeysToKeychain`; keys are passphraseless) — do not swap to `UseKeychain`.

**Changed Files** (WIP branch `wip/handoff-20260706-1105`, commit `aae6ffc`)
- `.claude/agents/*`, `.claude/skills/*`, `.claude/rules/model-routing.md`, `.claude/settings.json` — agent-environment scaffolding being installed.
- `.codex/*` — mirrored Codex prompts/profile.
- `AGENTS.md`, `CLAUDE.md`, `docs/{agent-handoff,verification-evidence,change-checklist,task-inbox}` — methodology docs (+ dated `.backup-*` copies).
- Feature code (Phases 3/4) already landed in earlier commits on `feature/v1.0-release-readiness` (through `8247ca0`); not part of this checkpoint.

**Verification**
- `dotnet test …Tests.csproj -c Release` (net8.0, `DOTNET_ROLL_FORWARD=Major`) — **PASS** (26/26).
- Windows CI compile of `8247ca0` — **PASS** (run `28714260647`).
- Phase 5 hardware checks — **BLOCKED** (no AMD GPU in this environment).

**Blockers / Residual Risk** — Phase 5 hardware verification unproven (fan ramp/hold at 65 °C, emergency 85%, exit→Adrenalin auto, both GPUs restore, no-admin fan set). All ADLX/ADL native paths are CI-compiled only.

**Next Safe Action** — Run Phase 5 hardware verification on the AMD PC per `docs/verification-evidence.md`; if all pass, cut the `v1.0.0` tag.
